-- input
do
  do
    do
      some_long_variable_name = table_ref[config.settings.deeply_nested.property] + other_ref[state.values.count]

-- expect
do
  do
    do
      some_long_variable_name = table_ref[config.settings.deeply_nested.property] +
         other_ref[state.values.count]