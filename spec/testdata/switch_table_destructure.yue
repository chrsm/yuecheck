-- input
do
	switch tb
		when [ [item,],]
			print item
		when [a = 1, b = "abc"]
			print a, b

-- expect
do
  switch tb
    when [ [ item, ], ]
      print item
    when [ a = 1, b = 'abc' ]
      print a, b
