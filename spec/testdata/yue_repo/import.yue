-- input
import hello from yeah
import holla, world from table["cool"]

import x, \y, z from items

import master, \ghost from find "mytable"

_obj_0 = 232

import something from a table


if indent
	import okay, \well from tables[100]

do
	import a, b, c from z

do
	import a,
		b, c from z

-- NOTE(@chrsm): modified original, as original syntax not valid
-- original was `do\n  import a\n  b...`
do
	import 
		a
		b
		c from z

do
	import
		a
		b
		c from z


do
	import
		a
		b
		c
		from z

do
	import p from "yue"
	import item from 'module.part'
	import x1, y1, \z1 from "mymodule"

do
	from 'yue' import p
	from 'mymodule' import x1, y1, \z1

do
	from z import a, b, c

do
	from z import a,
		b, c

-- NOTE(@chrsm): modified original, as original syntax not valid
-- original was `do\n  from z import a\n  b...`
do
	from z import 
		a
		b
		c

do
	from z import
		a
		b
		c

do
	from z
	import
		a
		b
		c

do
	from UnityEngine import Object
		GameObject
		Transform
		MonoBehaviour
		Vector3
		Quaternion

	speed = 10

do
	from UnityEngine import Object, GameObject,
		Transform
		MonoBehaviour
		Vector3
		Quaternion

	speed = 10

do
	from UnityEngine import Object, GameObject,
		Transform,
		MonoBehaviour
		Vector3, Quaternion

	update speed

do
	from UnityEngine import
		Object, GameObject,
		Transform,
		MonoBehaviour
		Vector3, Quaternion

	update speed

do
	import 'module'
	import 'module_x'
	import "d-a-s-h-e-s"
	import "module.part"

do
	import "player" as Player
	import "lpeg" as {:C, :Ct, :Cmt}
	import "export" as {one, two, Something:{umm:{ch}}}
	import "export" as :Another

do
	global *
	import 'module'
	import 'module_x'
	import "org.package.module-y"

do
	import "org.package.module" as function:func, if:ifVar

do
	import "m" as {<gc>: b}
	import "m" as {:<index>}
	import "m" as {e: f, <pairs>: c}
	import "m" as {c: d}
	import "m" as {g, {<close>: i}}

do
	import require
	import string as stringlib
	import string.format
	import io.read as io_read

	type = ->
	import type as tp
	import 月 as yue

-- expect
import hello from yeah
import holla, world from table['cool']

import x, :y, z from items

import master, :ghost from find 'mytable'

_obj_0 = 232

import something from a table

if indent
  import okay, :well from tables[100]

do
  import a, b, c from z

do
  import a, b, c from z

-- NOTE(@chrsm): modified original, as original syntax not valid
-- original was `do\n  import a\n  b...`
do
  import a, b, c from z

do
  import a, b, c from z

do
  import a, b, c from z

do
  import p from 'yue'
  import item from 'module.part'
  import x1, y1, :z1 from 'mymodule'

do
  from 'yue' import p
  from 'mymodule' import x1, y1, :z1

do
  from z import a, b, c

do
  from z import a, b, c

-- NOTE(@chrsm): modified original, as original syntax not valid
-- original was `do\n  from z import a\n  b...`
do
  from z import a, b, c

do
  from z import a, b, c

do
  from z import a, b, c

do
  from UnityEngine import Object, GameObject, Transform, MonoBehaviour, Vector3, Quaternion

  speed = 10

do
  from UnityEngine import Object, GameObject, Transform, MonoBehaviour, Vector3, Quaternion

  speed = 10

do
  from UnityEngine import Object, GameObject, Transform, MonoBehaviour, Vector3, Quaternion

  update speed

do
  from UnityEngine import Object, GameObject, Transform, MonoBehaviour, Vector3, Quaternion

  update speed

do
  import 'module'
  import 'module_x'
  import 'd-a-s-h-e-s'
  import 'module.part'

do
  import 'player' as Player
  import 'lpeg' as { :C, :Ct, :Cmt }
  import 'export' as { one, two, Something: { umm: { ch } } }
  import 'export' as { :Another }

do
  global *
  import 'module'
  import 'module_x'
  import 'org.package.module-y'

do
  import 'org.package.module' as { function: func, if: ifVar }

do
  import 'm' as { <gc>: b }
  import 'm' as { :<index> }
  import 'm' as { e: f, <pairs>: c }
  import 'm' as { c: d }
  import 'm' as { g, { <close>: i } }

do
  import
  import
  import
  import

  type = ->
  import
  import
